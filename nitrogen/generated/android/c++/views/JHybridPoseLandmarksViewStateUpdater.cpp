///
/// JHybridPoseLandmarksViewStateUpdater.cpp
/// This file was generated by nitrogen. DO NOT MODIFY THIS FILE.
/// https://github.com/mrousavy/nitro
/// Copyright © Marc Rousavy @ Margelo
///

#include "JHybridPoseLandmarksViewStateUpdater.hpp"
#include "views/HybridPoseLandmarksViewComponent.hpp"
#include <NitroModules/NitroDefines.hpp>
#include <react/fabric/StateWrapperImpl.h>

namespace margelo::nitro::poselandmarks::views {

using namespace facebook;
using ConcreteStateData = react::ConcreteState<HybridPoseLandmarksViewState>;

void JHybridPoseLandmarksViewStateUpdater::updateViewProps(jni::alias_ref<jni::JClass> /* class */,
                                           jni::alias_ref<JHybridPoseLandmarksViewSpec::JavaPart> javaView,
                                           jni::alias_ref<JStateWrapper::javaobject> stateWrapperInterface) {
  std::shared_ptr<JHybridPoseLandmarksViewSpec> hybridView = javaView->getJHybridPoseLandmarksViewSpec();

  // Get concrete StateWrapperImpl from passed StateWrapper interface object
  jobject rawStateWrapper = stateWrapperInterface.get();
  if (!stateWrapperInterface->isInstanceOf(react::StateWrapperImpl::javaClassStatic())) [[unlikely]] {
      throw std::runtime_error("StateWrapper is not a StateWrapperImpl");
  }
  auto stateWrapper = jni::alias_ref<react::StateWrapperImpl::javaobject>{
            static_cast<react::StateWrapperImpl::javaobject>(rawStateWrapper)};
  std::shared_ptr<const react::State> state = stateWrapper->cthis()->getState();
  auto concreteState = std::static_pointer_cast<const ConcreteStateData>(state);
  const HybridPoseLandmarksViewState& data = concreteState->getData();
  const std::shared_ptr<HybridPoseLandmarksViewProps>& props = data.getProps();
  if (props == nullptr) [[unlikely]] {
    // Props aren't set yet!
    throw std::runtime_error("HybridPoseLandmarksViewState's data doesn't contain any props!");
  }

  // Update all props if they are dirty
  if (props->isActive.isDirty) {
    hybridView->setIsActive(props->isActive.value);
    props->isActive.isDirty = false;
  }
  if (props->enableSkeleton.isDirty) {
    hybridView->setEnableSkeleton(props->enableSkeleton.value);
    props->enableSkeleton.isDirty = false;
  }
  if (props->skeletonColor.isDirty) {
    hybridView->setSkeletonColor(props->skeletonColor.value);
    props->skeletonColor.isDirty = false;
  }
  if (props->skeletonBoneThickness.isDirty) {
    hybridView->setSkeletonBoneThickness(props->skeletonBoneThickness.value);
    props->skeletonBoneThickness.isDirty = false;
  }
  if (props->landmarkColor.isDirty) {
    hybridView->setLandmarkColor(props->landmarkColor.value);
    props->landmarkColor.isDirty = false;
  }
  if (props->minVisibilityConfidence.isDirty) {
    hybridView->setMinVisibilityConfidence(props->minVisibilityConfidence.value);
    props->minVisibilityConfidence.isDirty = false;
  }
  if (props->modelSelection.isDirty) {
    hybridView->setModelSelection(props->modelSelection.value);
    props->modelSelection.isDirty = false;
  }
  if (props->delegateSelection.isDirty) {
    hybridView->setDelegateSelection(props->delegateSelection.value);
    props->delegateSelection.isDirty = false;
  }
  if (props->inferenceSampleRateHz.isDirty) {
    hybridView->setInferenceSampleRateHz(props->inferenceSampleRateHz.value);
    props->inferenceSampleRateHz.isDirty = false;
  }
  if (props->enableVisibilityRecovery.isDirty) {
    hybridView->setEnableVisibilityRecovery(props->enableVisibilityRecovery.value);
    props->enableVisibilityRecovery.isDirty = false;
  }
  if (props->enableOneEuroFilter.isDirty) {
    hybridView->setEnableOneEuroFilter(props->enableOneEuroFilter.value);
    props->enableOneEuroFilter.isDirty = false;
  }
  if (props->enableMotionPrediction.isDirty) {
    hybridView->setEnableMotionPrediction(props->enableMotionPrediction.value);
    props->enableMotionPrediction.isDirty = false;
  }
  if (props->oneEuroMinCutoff.isDirty) {
    hybridView->setOneEuroMinCutoff(props->oneEuroMinCutoff.value);
    props->oneEuroMinCutoff.isDirty = false;
  }
  if (props->oneEuroBeta.isDirty) {
    hybridView->setOneEuroBeta(props->oneEuroBeta.value);
    props->oneEuroBeta.isDirty = false;
  }
  if (props->width.isDirty) {
    hybridView->setWidth(props->width.value);
    props->width.isDirty = false;
  }
  if (props->height.isDirty) {
    hybridView->setHeight(props->height.value);
    props->height.isDirty = false;
  }

  // Update hybridRef if it changed
  if (props->hybridRef.isDirty) {
    // hybridRef changed - call it with new this
    const auto& maybeFunc = props->hybridRef.value;
    if (maybeFunc.has_value()) {
      maybeFunc.value()(hybridView);
    }
    props->hybridRef.isDirty = false;
  }
}

} // namespace margelo::nitro::poselandmarks::views
