///
/// HybridPoseLandmarksViewComponent.cpp
/// This file was generated by nitrogen. DO NOT MODIFY THIS FILE.
/// https://github.com/mrousavy/nitro
/// Copyright © Marc Rousavy @ Margelo
///

#include "HybridPoseLandmarksViewComponent.hpp"

#include <string>
#include <exception>
#include <utility>
#include <NitroModules/NitroDefines.hpp>
#include <NitroModules/JSIConverter.hpp>
#include <NitroModules/PropNameIDCache.hpp>
#include <react/renderer/core/RawValue.h>
#include <react/renderer/core/ShadowNode.h>
#include <react/renderer/core/ComponentDescriptor.h>
#include <react/renderer/components/view/ViewProps.h>

namespace margelo::nitro::poselandmarks::views {

  extern const char HybridPoseLandmarksViewComponentName[] = "PoseLandmarksView";

  HybridPoseLandmarksViewProps::HybridPoseLandmarksViewProps(const react::PropsParserContext& context,
                                                             const HybridPoseLandmarksViewProps& sourceProps,
                                                             const react::RawProps& rawProps):
    react::ViewProps(context, sourceProps, rawProps, filterObjectKeys),
    isActive([&]() -> CachedProp<bool> {
      try {
        const react::RawValue* rawValue = rawProps.at("isActive", nullptr, nullptr);
        if (rawValue == nullptr) return sourceProps.isActive;
        const auto& [runtime, value] = (std::pair<jsi::Runtime*, jsi::Value>)*rawValue;
        return CachedProp<bool>::fromRawValue(*runtime, value, sourceProps.isActive);
      } catch (const std::exception& exc) {
        throw std::runtime_error(std::string("PoseLandmarksView.isActive: ") + exc.what());
      }
    }()),
    enableSkeleton([&]() -> CachedProp<bool> {
      try {
        const react::RawValue* rawValue = rawProps.at("enableSkeleton", nullptr, nullptr);
        if (rawValue == nullptr) return sourceProps.enableSkeleton;
        const auto& [runtime, value] = (std::pair<jsi::Runtime*, jsi::Value>)*rawValue;
        return CachedProp<bool>::fromRawValue(*runtime, value, sourceProps.enableSkeleton);
      } catch (const std::exception& exc) {
        throw std::runtime_error(std::string("PoseLandmarksView.enableSkeleton: ") + exc.what());
      }
    }()),
    skeletonColor([&]() -> CachedProp<std::string> {
      try {
        const react::RawValue* rawValue = rawProps.at("skeletonColor", nullptr, nullptr);
        if (rawValue == nullptr) return sourceProps.skeletonColor;
        const auto& [runtime, value] = (std::pair<jsi::Runtime*, jsi::Value>)*rawValue;
        return CachedProp<std::string>::fromRawValue(*runtime, value, sourceProps.skeletonColor);
      } catch (const std::exception& exc) {
        throw std::runtime_error(std::string("PoseLandmarksView.skeletonColor: ") + exc.what());
      }
    }()),
    skeletonBoneThickness([&]() -> CachedProp<double> {
      try {
        const react::RawValue* rawValue = rawProps.at("skeletonBoneThickness", nullptr, nullptr);
        if (rawValue == nullptr) return sourceProps.skeletonBoneThickness;
        const auto& [runtime, value] = (std::pair<jsi::Runtime*, jsi::Value>)*rawValue;
        return CachedProp<double>::fromRawValue(*runtime, value, sourceProps.skeletonBoneThickness);
      } catch (const std::exception& exc) {
        throw std::runtime_error(std::string("PoseLandmarksView.skeletonBoneThickness: ") + exc.what());
      }
    }()),
    landmarkColor([&]() -> CachedProp<std::string> {
      try {
        const react::RawValue* rawValue = rawProps.at("landmarkColor", nullptr, nullptr);
        if (rawValue == nullptr) return sourceProps.landmarkColor;
        const auto& [runtime, value] = (std::pair<jsi::Runtime*, jsi::Value>)*rawValue;
        return CachedProp<std::string>::fromRawValue(*runtime, value, sourceProps.landmarkColor);
      } catch (const std::exception& exc) {
        throw std::runtime_error(std::string("PoseLandmarksView.landmarkColor: ") + exc.what());
      }
    }()),
    minVisibilityConfidence([&]() -> CachedProp<double> {
      try {
        const react::RawValue* rawValue = rawProps.at("minVisibilityConfidence", nullptr, nullptr);
        if (rawValue == nullptr) return sourceProps.minVisibilityConfidence;
        const auto& [runtime, value] = (std::pair<jsi::Runtime*, jsi::Value>)*rawValue;
        return CachedProp<double>::fromRawValue(*runtime, value, sourceProps.minVisibilityConfidence);
      } catch (const std::exception& exc) {
        throw std::runtime_error(std::string("PoseLandmarksView.minVisibilityConfidence: ") + exc.what());
      }
    }()),
    modelSelection([&]() -> CachedProp<double> {
      try {
        const react::RawValue* rawValue = rawProps.at("modelSelection", nullptr, nullptr);
        if (rawValue == nullptr) return sourceProps.modelSelection;
        const auto& [runtime, value] = (std::pair<jsi::Runtime*, jsi::Value>)*rawValue;
        return CachedProp<double>::fromRawValue(*runtime, value, sourceProps.modelSelection);
      } catch (const std::exception& exc) {
        throw std::runtime_error(std::string("PoseLandmarksView.modelSelection: ") + exc.what());
      }
    }()),
    inferenceSampleRateHz([&]() -> CachedProp<double> {
      try {
        const react::RawValue* rawValue = rawProps.at("inferenceSampleRateHz", nullptr, nullptr);
        if (rawValue == nullptr) return sourceProps.inferenceSampleRateHz;
        const auto& [runtime, value] = (std::pair<jsi::Runtime*, jsi::Value>)*rawValue;
        return CachedProp<double>::fromRawValue(*runtime, value, sourceProps.inferenceSampleRateHz);
      } catch (const std::exception& exc) {
        throw std::runtime_error(std::string("PoseLandmarksView.inferenceSampleRateHz: ") + exc.what());
      }
    }()),
    enableVisibilityRecovery([&]() -> CachedProp<bool> {
      try {
        const react::RawValue* rawValue = rawProps.at("enableVisibilityRecovery", nullptr, nullptr);
        if (rawValue == nullptr) return sourceProps.enableVisibilityRecovery;
        const auto& [runtime, value] = (std::pair<jsi::Runtime*, jsi::Value>)*rawValue;
        return CachedProp<bool>::fromRawValue(*runtime, value, sourceProps.enableVisibilityRecovery);
      } catch (const std::exception& exc) {
        throw std::runtime_error(std::string("PoseLandmarksView.enableVisibilityRecovery: ") + exc.what());
      }
    }()),
    enableOneEuroFilter([&]() -> CachedProp<bool> {
      try {
        const react::RawValue* rawValue = rawProps.at("enableOneEuroFilter", nullptr, nullptr);
        if (rawValue == nullptr) return sourceProps.enableOneEuroFilter;
        const auto& [runtime, value] = (std::pair<jsi::Runtime*, jsi::Value>)*rawValue;
        return CachedProp<bool>::fromRawValue(*runtime, value, sourceProps.enableOneEuroFilter);
      } catch (const std::exception& exc) {
        throw std::runtime_error(std::string("PoseLandmarksView.enableOneEuroFilter: ") + exc.what());
      }
    }()),
    enableMotionPrediction([&]() -> CachedProp<bool> {
      try {
        const react::RawValue* rawValue = rawProps.at("enableMotionPrediction", nullptr, nullptr);
        if (rawValue == nullptr) return sourceProps.enableMotionPrediction;
        const auto& [runtime, value] = (std::pair<jsi::Runtime*, jsi::Value>)*rawValue;
        return CachedProp<bool>::fromRawValue(*runtime, value, sourceProps.enableMotionPrediction);
      } catch (const std::exception& exc) {
        throw std::runtime_error(std::string("PoseLandmarksView.enableMotionPrediction: ") + exc.what());
      }
    }()),
    oneEuroMinCutoff([&]() -> CachedProp<double> {
      try {
        const react::RawValue* rawValue = rawProps.at("oneEuroMinCutoff", nullptr, nullptr);
        if (rawValue == nullptr) return sourceProps.oneEuroMinCutoff;
        const auto& [runtime, value] = (std::pair<jsi::Runtime*, jsi::Value>)*rawValue;
        return CachedProp<double>::fromRawValue(*runtime, value, sourceProps.oneEuroMinCutoff);
      } catch (const std::exception& exc) {
        throw std::runtime_error(std::string("PoseLandmarksView.oneEuroMinCutoff: ") + exc.what());
      }
    }()),
    oneEuroBeta([&]() -> CachedProp<double> {
      try {
        const react::RawValue* rawValue = rawProps.at("oneEuroBeta", nullptr, nullptr);
        if (rawValue == nullptr) return sourceProps.oneEuroBeta;
        const auto& [runtime, value] = (std::pair<jsi::Runtime*, jsi::Value>)*rawValue;
        return CachedProp<double>::fromRawValue(*runtime, value, sourceProps.oneEuroBeta);
      } catch (const std::exception& exc) {
        throw std::runtime_error(std::string("PoseLandmarksView.oneEuroBeta: ") + exc.what());
      }
    }()),
    width([&]() -> CachedProp<double> {
      try {
        const react::RawValue* rawValue = rawProps.at("width", nullptr, nullptr);
        if (rawValue == nullptr) return sourceProps.width;
        const auto& [runtime, value] = (std::pair<jsi::Runtime*, jsi::Value>)*rawValue;
        return CachedProp<double>::fromRawValue(*runtime, value, sourceProps.width);
      } catch (const std::exception& exc) {
        throw std::runtime_error(std::string("PoseLandmarksView.width: ") + exc.what());
      }
    }()),
    height([&]() -> CachedProp<double> {
      try {
        const react::RawValue* rawValue = rawProps.at("height", nullptr, nullptr);
        if (rawValue == nullptr) return sourceProps.height;
        const auto& [runtime, value] = (std::pair<jsi::Runtime*, jsi::Value>)*rawValue;
        return CachedProp<double>::fromRawValue(*runtime, value, sourceProps.height);
      } catch (const std::exception& exc) {
        throw std::runtime_error(std::string("PoseLandmarksView.height: ") + exc.what());
      }
    }()),
    hybridRef([&]() -> CachedProp<std::optional<std::function<void(const std::shared_ptr<HybridPoseLandmarksViewSpec>& /* ref */)>>> {
      try {
        const react::RawValue* rawValue = rawProps.at("hybridRef", nullptr, nullptr);
        if (rawValue == nullptr) return sourceProps.hybridRef;
        const auto& [runtime, value] = (std::pair<jsi::Runtime*, jsi::Value>)*rawValue;
        return CachedProp<std::optional<std::function<void(const std::shared_ptr<HybridPoseLandmarksViewSpec>& /* ref */)>>>::fromRawValue(*runtime, value.asObject(*runtime).getProperty(*runtime, PropNameIDCache::get(*runtime, "f")), sourceProps.hybridRef);
      } catch (const std::exception& exc) {
        throw std::runtime_error(std::string("PoseLandmarksView.hybridRef: ") + exc.what());
      }
    }()) { }

  bool HybridPoseLandmarksViewProps::filterObjectKeys(const std::string& propName) {
    switch (hashString(propName)) {
      case hashString("isActive"): return true;
      case hashString("enableSkeleton"): return true;
      case hashString("skeletonColor"): return true;
      case hashString("skeletonBoneThickness"): return true;
      case hashString("landmarkColor"): return true;
      case hashString("minVisibilityConfidence"): return true;
      case hashString("modelSelection"): return true;
      case hashString("inferenceSampleRateHz"): return true;
      case hashString("enableVisibilityRecovery"): return true;
      case hashString("enableOneEuroFilter"): return true;
      case hashString("enableMotionPrediction"): return true;
      case hashString("oneEuroMinCutoff"): return true;
      case hashString("oneEuroBeta"): return true;
      case hashString("width"): return true;
      case hashString("height"): return true;
      case hashString("hybridRef"): return true;
      default: return false;
    }
  }

  HybridPoseLandmarksViewComponentDescriptor::HybridPoseLandmarksViewComponentDescriptor(const react::ComponentDescriptorParameters& parameters)
    : ConcreteComponentDescriptor(parameters,
                                  react::RawPropsParser(/* enableJsiParser */ true)) {}

  std::shared_ptr<const react::Props> HybridPoseLandmarksViewComponentDescriptor::cloneProps(const react::PropsParserContext& context,
                                                                                             const std::shared_ptr<const react::Props>& props,
                                                                                             react::RawProps rawProps) const {
    // 1. Prepare raw props parser
    rawProps.parse(rawPropsParser_);
    // 2. Copy props with Nitro's cached copy constructor
    return HybridPoseLandmarksViewShadowNode::Props(context, /* & */ rawProps, props);
  }

#ifdef ANDROID
  void HybridPoseLandmarksViewComponentDescriptor::adopt(react::ShadowNode& shadowNode) const {
    // This is called immediately after `ShadowNode` is created, cloned or in progress.
    // On Android, we need to wrap props in our state, which gets routed through Java and later unwrapped in JNI/C++.
    auto& concreteShadowNode = static_cast<HybridPoseLandmarksViewShadowNode&>(shadowNode);
    const std::shared_ptr<const HybridPoseLandmarksViewProps>& constProps = concreteShadowNode.getConcreteSharedProps();
    const std::shared_ptr<HybridPoseLandmarksViewProps>& props = std::const_pointer_cast<HybridPoseLandmarksViewProps>(constProps);
    HybridPoseLandmarksViewState state{props};
    concreteShadowNode.setStateData(std::move(state));
  }
#endif

} // namespace margelo::nitro::poselandmarks::views
